interface:
  display_name: "Creative Testing"
  short_description: "Design disciplined social creative experiments."
  default_prompt: "Use $creative-testing to turn our current campaign into a controlled creative test plan and draft variants."

dependencies:
  tools:
    - type: "mcp"
      value: "simplified"
      description: "Simplified hosted MCP connector for creative generation, social drafts, and analytics."
      transport: "http"
      url: "https://apikit.simplified.com/mcp"

policy:
  allow_implicit_invocation: true
